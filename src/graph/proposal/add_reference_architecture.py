from src.graph.state import CustomerState


async def add_reference_architecture(state: CustomerState) -> dict:
    """Embed architecture from PoC phase with Mermaid diagrams. interrupt()."""
    return {"last_node": "add_reference_architecture"}
