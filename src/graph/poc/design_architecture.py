from src.graph.state import CustomerState


async def design_architecture(state: CustomerState) -> dict:
    """Design Tower-based target architecture from tech_env + use_cases. interrupt()."""
    return {"last_node": "design_architecture"}
